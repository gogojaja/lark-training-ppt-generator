# 范围初定义说明书

**项目编号**：PRJ-002  
**项目名称**：企业培训PPT生成器  
**定义日期**：2026-08-14  
**版本**：v1.0  
**状态**：初步定义

---

## 1. 范围概述

本项目旨在通过AI自动化工具，将企业培训内容快速转换为符合企业标准格式的PPT。本说明书定义了项目的初步范围，作为后续需求分析和开发的基准。后续阶段将对范围进行细化和完善。

---

## 2. 交付边界（做什么）

本项目将包含以下功能：

### 2.1 文本输入自动转PPT
- **支持输入源**：文本、文档（Word、PDF）、网页等
- **内容提取**：自动识别主题、结构、要点
- **结构化处理**：AI理解内容语义，自动组织成PPT结构
- **输出格式**：结构化的PPT内容（封面、目录、章节、要点、总结）

### 2.2 多场景模板库
- **场景覆盖**：
  - 培训课件：适用于企业内部培训、外部培训
  - 制度手册宣讲：适用于规章制度宣讲、政策解读
  - 会议汇报：适用于周会、月会、季度汇报
  - 产品介绍：适用于新产品发布、产品演示
- **模板类型**：
  - 商务模板：简洁、专业、大气
  - 创意模板：活泼、创新、现代
  - 学术模板：严谨、规范、清晰
- **模板数量**：至少5个典型场景模板

### 2.3 内容结构化处理
- **AI理解**：语义分析、实体识别、关系抽取
- **结构生成**：自动生成PPT大纲、章节划分、要点提炼
- **内容优化**：语言润色、逻辑重组、重点突出

### 2.4 PPT生成与编辑
- **生成功能**：支持从文本/文档自动生成完整PPT
- **编辑功能**：支持单页编辑、批量编辑、模板切换
- **样式调整**：支持字体、颜色、版式、间距等样式调整
- **批量生成**：支持批量生成多个PPT（如多个培训章节）

### 2.5 内容校验
- **格式检查**：字体、颜色、版式、动画等格式规范检查
- **错别字检查**：自动检查文本中的错别字、拼写错误
- **内容完整性**：检查是否有遗漏的章节、要点
- **逻辑一致性**：检查内容逻辑是否通顺、一致
- **准确性检查**：AI辅助检查内容准确性、专业性

### 2.6 版本管理
- **版本保存**：支持保存多个版本，版本命名可自定义
- **版本对比**：支持版本对比，高亮差异
- **版本回滚**：支持版本回滚到任意历史版本
- **版本分享**：支持版本分享，导出版本文件

### 2.7 用户界面
- **操作方式**：支持拖拽、批量操作、快捷键等操作方式
- **可视化编辑**：支持所见即所得的PPT编辑
- **快捷操作**：常用操作快捷键，提高操作效率
- **帮助文档**：提供操作指引和帮助文档

### 2.8 导出功能
- **导出格式**：支持导出为PPTX、PDF等格式
- **导出选项**：支持自定义导出选项（如页面范围、质量、压缩等）
- **批量导出**：支持批量导出多个PPT

### 2.9 内容自查
- **准确性自查**：AI检查内容是否准确、专业
- **逻辑性自查**：AI检查内容逻辑是否通顺、合理
- **专业性自查**：AI检查内容是否符合企业专业标准

### 2.10 配置管理
- **用户配置**：支持用户自定义模板、样式、规则等配置
- **团队配置**：支持团队共享配置，统一标准
- **配置导入导出**：支持配置的导入导出，便于迁移和备份

---

## 3. 排除项（不做什么）

本项目将不包含以下内容：

### 3.1 不包含原创内容创作
- **原因**：本项目专注于内容理解和结构化，不生成原创内容
- **说明**：仅做内容提取、重组、优化，不创作新的内容

### 3.2 不包含高级动画效果
- **排除功能**：
  - 复杂动画（如路径动画、3D动画、旋转动画等）
  - 高级特效（如闪烁、渐变、扭曲等）
  - 交互动画（如点击触发、鼠标悬停等）
- **保留功能**：基础动画（淡入、淡出、滑入、滑出等）

### 3.3 不包含音视频素材处理
- **排除功能**：
  - 音频剪辑、混音、降噪
  - 视频剪辑、转码、特效
  - 字幕生成、添加
  - 音视频混合、嵌入
- **保留功能**：仅支持文本内容，不支持音视频素材

### 3.4 不包含第三方商业模板
- **原因**：商业模板可能涉及版权问题，且不符合开源优先原则
- **说明**：仅使用开源或自研模板，不集成商业模板库

### 3.5 不包含实时协作
- **排除功能**：
  - 多人同时编辑
  - 实时同步
  - 在线协作评论
  - 协作历史记录
- **说明**：本项目为单用户工具，不支持多人协作

### 3.6 不包含云存储集成
- **排除功能**：
  - 直接上传到云存储服务（如Google Drive、OneDrive、百度网盘等）
  - 云端同步
  - 云端协作
- **说明**：本项目必须在本地部署，不依赖云服务

### 3.7 不包含AI模型微调
- **排除功能**：
  - AI模型训练
  - AI模型微调
  - AI模型优化
  - AI模型部署
- **说明**：仅使用预训练模型，不包含模型训练和优化功能

### 3.8 不包含高级数据分析
- **排除功能**：
  - PPT内容深度分析
  - 数据统计和可视化
  - 内容趋势分析
  - 用户行为分析
- **说明**：本项目专注于PPT生成，不支持对PPT内容进行深度分析

---

## 4. 假设与制约

### 4.1 团队假设
1. **团队成员具备AI开发经验**：假设团队成员熟悉Python、Node.js等编程语言，熟悉AI框架（如PyTorch、TensorFlow），能够快速上手开发
2. **团队成员熟悉PPT操作**：假设团队成员熟悉PPT的基本操作，能够理解用户需求，设计合理的交互方式
3. **团队成员具备学习能力**：假设团队成员能够快速学习新技术、新工具，适应项目需求变化

### 4.2 环境假设
1. **本地部署环境支持**：假设本地部署环境支持所需技术栈运行（Python、Node.js、Git等）
2. **AI模型运行环境**：假设本地环境能够满足AI模型运行要求（如GPU、内存、存储等）
3. **网络环境**：假设本地网络环境稳定，能够访问必要的开源资源（如GitHub、NPM等）

### 4.3 用户假设
1. **用户接受AI工具**：假设企业用户能够接受使用AI工具生成PPT，愿意配合人工审核和调整
2. **用户具备基本操作能力**：假设用户具备基本的计算机操作能力，能够使用PPT软件
3. **用户有明确的格式规范**：假设企业有统一的PPT格式规范，能够指导AI生成符合要求的PPT

### 4.4 业务假设
1. **定期更新需求**：假设企业需要定期更新培训内容，AI工具能够满足这种动态需求
2. **场景多样化**：假设培训场景多样化，AI工具能够适应不同场景的需求
3. **有统一的格式标准**：假设企业有统一的PPT格式规范，能够指导AI生成符合要求的PPT

---

## 5. 变更管理流程

### 5.1 变更触发条件
以下情况触发范围变更：
- 需求方提出新的功能需求
- 技术可行性发生变化
- 约束条件发生变化
- 用户反馈需要调整范围

### 5.2 变更流程
1. **变更申请**：干系人提出变更需求，填写变更申请单
2. **影响分析**：项目经理评估变更对范围、进度、成本、质量的影响
3. **审批决策**：项目经理决策（批准/拒绝/暂缓），记录决策依据
4. **变更实施**：更新范围定义，调整项目计划
5. **记录留痕**：记录变更原因、影响分析、决策依据、变更内容
6. **沟通通知**：通知所有干系人变更内容，确保信息同步

### 5.3 变更审计
范围变更必须经过 ProjectMonitorSkill `change_audit` 审计，记录：
- 变更原因
- 影响分析
- 决策依据
- 变更内容
- 变更结果

---

## 6. 范围固化原则

### 6.1 范围固化时机
- **初步固化**：本说明书定义的初步范围，作为需求分析阶段的基准
- **正式固化**：需求分析完成后，经干系人确认的范围，作为开发和测试阶段的基准
- **变更固化**：范围变更后，经审批和审计的范围，作为最新基准

### 6.2 范围固化约束
- **未经审批，不得固化**：任何范围变更必须经过审批和审计
- **未经审计，不得固化**：任何范围变更必须经过 ProjectMonitorSkill `change_audit` 审计
- **固化后，不得随意变更**：固化后的范围是开发和测试的基准，不得随意变更

---

## 7. 范围治理边界（2026-08-18 固）

### 7.1 边界原则
本项目（`lark-training-ppt-generator`）的工作范围**严格限定于本仓库**
`https://github.com/gogojaja/lark-training-ppt-generator.git`。

### 7.2 外部工具独立库清单
以下工具/技能**不属于本项目范围**，由独立仓库管理维护，本项目仅保留声明式引用，
不含副本、不直接维护：

| 外部工具 | 独立仓库 | 本项目内的引用方式 |
|----------|----------|-------------------|
| DevProjectTeamSkill（编排器） | `https://github.com/gogojaja/DevProjectTeamSkill.git` | 只读消费，不修改不提交；本项目内不内嵌副本 |
| lark-shared（认证/权限/JSON 契约） | 独立仓库（待建） | `skills/SKILL.md` 声明式引用，clone 到项目同级目录后可用 |
| lark-slides（幻灯片创建） | 独立仓库（待建） | 同上 |
| lark-doc（文档读取） | 独立仓库（待建） | 同上 |
| lark-wiki（知识库发布） | 独立仓库（待建） | 同上 |
| lark-base（签到/反馈表） | 独立仓库（待建） | 同上 |

### 7.3 边界约束
1. 本项目内禁止提交外部工具副本（含 `.github/skills/` 内嵌拷贝）；
2. 本项目内禁止硬编码外部工具的绝对路径（如 `D:\trae\lark-*`），统一使用相对引用或
   独立仓库 clone 路径；
3. 外部工具升级由各自独立仓库管理，本项目不复制、不随包分发其源码；
4. 若外部工具尚未建库（如 lark-* 5 技能），使用前需先创建独立仓库并 clone 到项目
   同级目录，使其相对引用 `../lark-*` 可用。

### 7.4 变更管理
本节边界变更须经 `change_audit` 审计（对齐 §5），记录变更原因、影响分析与决策依据。

---

## 8. 签署确认

**定义人**：段波  
**确认日期**：2026-08-14  
**边界固化确认**：2026-08-18

---

**文档版本**：v1.1  
**最后更新**：2026-08-18（新增 §7 范围治理边界：工作范围限定于本仓库，外部工具由独立库管理）
**知识产权所有**：段波（验证邮箱：duanbo.douglas@163.com）